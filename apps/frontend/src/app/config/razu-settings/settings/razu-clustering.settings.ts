import { ClusteringSettings } from '../../../models/settings/clustering-settings.model';
import { clusteringSettings } from '../../default-settings/settings/clustering.settings';

export const razuClusteringSettings: ClusteringSettings = {
  ...clusteringSettings,
};
